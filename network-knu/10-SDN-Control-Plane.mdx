# SDN
인터터넷 네트워크 레이어는 분산돼었고 각 라우팅 알고맂즘을 돌렸다.

그래서 비쌋음

하지만 2005년부터 network control plane에 대해 사람들이 관심을 가지게 돼면서(연구가 됌)

그
그래서 각 라우터와 라우터가 라우팅 알고리즘을 돌려서 연결정보를 주고 받고 각각의 포워딜 테이블을 만들었다. 비싸고 성능도 좋아야했음

BGP는 유투브는 저기서 네이이버는 저기로 이런게 절대 불가능, 그냥 이길은 가지마 이정도만 가능

하지만 SDN에선 컨트트롤플랜과 데이터터플랜이나뉨, 데이터 플랜에선 포워딩 테이블만 함

openflow 는 포트번호를 볼수 있는데 이는 80번은 웹 22는ssh 이런식으로 나눌수 있다는것임

## 왜 중앙집중됀 플랜이 필요한가?
1. 관리하기 편함
    * 성적처리를 한다 했을때 혼자서 100명 체점보단 5명이서 100명 채점이 더 빠르다, 하지만 머지에 대한 비용이 발생한다, 분산시스템템의 제어는 복잡함
2. 유투브 트래픽은 저기로,웹 트래픽은 저기로 같은 관리가가능함
3. 목적지만 보는게 아니라 원하는델 보낼수 있음
4. Control plane가 오픈소스화 돼면서 폭발적으로 발전했음

## SDN analogy
예전엔 특정 하드웨어, 특정 os, 특정 앱 을 폐쇄적으로 팔아서 몇천만원대대의 가격에 판매했는데

요즘은 수평적으로 나누어져서 적당한 하드웨어 구하고 os 알아서 설치하고 앱도 알아서 설치하고가 됐음, 이게 전ㅂ부 open돼었기 때문엠(Open interface)

메인프레임 -> PC로의 발전은 혁명이다

## 트래픽
트래픽이란건 목적지를 보면 무조건 거기로 가야했음(라우팅에 따라 목적지를 변경해주긴 하지만)

하지만 SDN은 여기로 가면돼, 저기로 가면돼 가 가능하다.

4의 길과 10의 길이 있다면 전통적인 라우팅에선 절대 10으로 못가지만 상황에따라 10으로 갈수 있단 의미다.

로드 밸런싱이 가능하다


***
# Super vision
서로 통신하기 위한 API가 필요함


# SDNcontroller (network os)


# SDN
핵심은 언번들이란거임 그 누구도 이 모든것을 개발하진 않음, Open flow라는 공개된 프로토콜이 있기 때문문문임, 그래서 누구나 진입이 가능해 경쟁을 촉진 가능

***

# Components of SDN controller
* north bound
* SDN 컨트롤러
* South bound

XML은 html과 같은 포맷이이지만 xml은 사용자 정의로 포맷을 결정하고 애플리케이션과 소통 가능

***
# SDN: 계속 논의돼고 발전돼어야 하는 부분
안
안전성: 한놈이 죽으면 다음놈이 바로 이어받아 하는것, 보통 죽으면 안돼는 놈은 기본적으로 이중화가 기본임, 아니명 1:9로 하다가 9가 죽으면면1이 100% 돌아가겠금

블록체인이 죽은것처럼 SDN도 많이 죽었음, 구글은 내부 네트워크크가 SDN임

강원대도 트레디셔널라우팅 어쩌구 씀, 잘 돌아가는 시스템을 한번에 바꾸기 쉽지 않기 때문

***

# ICMP
Internet contrl msesage protocol

ping은 기본적으로 어던 시스템을 공격을하기 위해 해당 시스템의 ip를 알아야 함

그때 ping을 보내서 해당 시스템이 살았니 죽었니를 알아낸다 한다, 이래가지고 요즘 다 막막아놓음

방화벽 레벨에서 막았았을수도


***

# What is network management
웹서버가 동작하지 않은 이유에는 DB가 안돼냐, 앱이 안돼냐, 네트워크가 안돼냐 등 이 있음

그래서 어떤 문제가 발생한지 볼려면 모니터링을 해야함, 이 컨셉이 네트웤 메니지먼트의 큰 컨셉

autonomous system은 수천개의 하드웨어 컴포넌트 위체 수천개으ㅢ 소프트웨어 컴포넌트트가..

온도는 괜찮은지 방사능은 괜찮은지... [원자력 발전소]

기본적으로 하드웨어 모니터링인데 좀 나아가서 사람까지 관리함, 수강강신청 시스템 같은거

이래서 network management가 아닌 service management 라 하기도 함


## 구성요소
**웹서버 <-> 클라이언트**

data: 학생DB를 관리한다치면 학번,나이, 학과 같은 데이이터를 

lv2장비면 맥주소소가필요하고 lv3장비면 ip가 필필요함

agent: manegent devce: 관리돼는 장비

SNMP: HTTP 같은 프로토콜임

데이터는 데이터랑 비슷한데, DB엔 스키마가 있고 스키마엔 데이터가 있음, 학번(인트), 집주소VARCHAR(100), 이런런식으로 스키마에 타입같은거 적는거, 이름: 누구, 학번: 몇점, ... 이런식의 데이터 포맷이 있음, 참고로 SNMP는 MIB?임

SMI는 DB 스키마랑 똑똑같은거임, 근데 엄밀히는 속성이랑 똑같으늣

한 row가 이 MIB이 돼는는거임

SNMP만 한학기 이상 수업할만큼 어어려운데, 핵심만..

MIB2 가 있는데 모든 Network디바이스스가 가지고 있어야 함

SNMP는 HTTP, MIB은 HTML??? 이건 물어봐야 할듯

NETCONF는 XML 포맷으로 왔다갔다 한다 한다. 애초에 ENTDONFIGURETION의 약자자라서 바꿀라고 만들어진거임

trap message: 문제가 있을때 보고고하란 의미, 문제 없으면 가만히 있음

tmi: snmp2 에 보안기능이 들어갔는는데 실패함, 그래서 고가의 장비는snmp3가 있고대부분의 장비는 snmmp1이 들어감

교수가 snmp로 돌아다녔는데 학교에 db가 6개개정도 있는걸 알았다 한다. 그래서 장비를 구매했는데 snmp1이 있다면  disable 시켜라

기업들이 하도 만들어서 표준이 400개개란다.

MIB2만 있어서 UDP에 몇개 패패킷이 빠졌는지에 대한 모든통계를 알수있다.

주민번호가 있있어 누군지 특정할할수 있는것처럼 유니크한 값으로 MIB을 가질 수 있?다

NETCONF는 딱히 실용화돼지 못했음, 너무 무거워서.. SNMP로도 충분하지 않나? 마인드임

NETCONF는 주고받받는 메세지 포맷이 xml임

NETCONF의 message를 살펴보면 이더넷넷0 만 1500으로 바꾸는데 보내는 메세지의 양이 매우 많다다는것을 알수 있다. xml의 단점.. 하지지만 한번에 변경하하는게 가능하단 장점도 있다.







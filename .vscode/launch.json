{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "npm run dev",
      "command": "npm run dev",
      "cwd": "${workspaceFolder}"
    },
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "npm run build:ssg",
      "command": "npm run build:ssg",
      "cwd": "${workspaceFolder}"
    }
  ]
}
체스만 하더라도 기존의 Q-learning 같은 방식으론 전세계의 하드웨어를 끌어와야 할것

# Cart Pole
1. 각도 연속, 근데 1도씩 잘라서 180개로 만들어도 가능? 돼긴 하다.

2. 두번째로 Q-learning 라는걸 생각해보면 각 상태가 독립적인 성격이 강하지만, 이 문제 같은 경우는 1도만 움직여야 할때는? 근접값인까 방법이 있을거임


# Funcion
w: 연속적인실수

y = f(x) 같은거임?

결국 예전목표나 똑같이 Target과 예상치가 같게하는게 목표, error = 목표

파란 점들(실제 값들)과 Function Approximator(빨간점) 을 최대한 맞추는것이 학습, 학습이란건 결국 V ^= v, x 여기서 매개변수를 수정해 나가는것

# 기계학습
드디어 명시적인 기계학습 이란 단어가 등장

오늘부턴 지도학습을 해야함

***

# Work flow
경험들은 메모리에 저장

훈련: 별거없이 w를 찾는것

근데 지도학습에서의 훈련이란?

데이터셋이 : 입력고 출력

특성값: 이 각도 이상으론 필요 없다! 해서 버리는것, 딱히 규칙이 없고 결과만 잘나오면 장땡, 이전에 미로 문제에서 사방팔방 다 돌아다니는것도 일정의 특성값

초매개변수?: $w_0, w_1S*$ 이런 w0 같은것들은 학습이 안됄수가 있음 아니면 $w_1*S$ 만 써서 안쓸수도 있음, 그래서 직접 넣어야 하는것

***

일반 일반적인 기계학습에선 오프라인 학습: 주식 할때 실시간 정보를 추가 할려면 통째로 다시 넣어야함

하지만 강화학습에선 환경이 없기때문에(데이터 셋이 없어서) 온라인 학습 해야함, 이런 방법도 가능함, 100스탭까지 학습 안하고 데셋 생성만 한다음 100스탭 학습 그리고 200개 생성.. 모 dp처럼 비효율적이지만 가능하긴 함

***

# VE
이차함수가 매우 종요한 성질임, 그래서 성립함

미분해서 할순 있지만 비효율적이기 때문에 결국 이것도 근사값을 구하는거임

## 근사 하강법
엄청 흔한 최적화 문제, 벡터기 때문에 편미분 한거임

경사하강법: 온라인 학습 가능

그래서 100스탭씩 학습할수도 있음

***
# Non-Parametric

V^(S, W) 이런거 잊고 그냥 무지성으로 매개변수 없이 학습하는 방법

알고르짐상 w가 없는경우에 행동

메모리에 저장한다 하긴 햇는데 이 방법에선 sqlite, mysql, mongodb 도 많이씀

검색해서 대응돼는 상태들을 불러오고? 유사도에 따라 가중치를 두어 평균

이걸 좀 있어보이는말로 커널베이스드..

D: 모든 데이터, 상태

V ^= (s; D): 모든 상태에 대해 유사도를 활용? 검색?

그래서 데이터만 쌓아두고 쿼리만 날리는거임
